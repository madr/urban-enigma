import React from 'react'

export default props => {
  return (
    <nav id="nav">
      <button>📆</button>
      <button>➕</button>
      <button>📈</button>
      <button>⛄</button>
    </nav>
  )
}